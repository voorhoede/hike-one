language: node_js
git:
  depth: 1
node_js:
- '8'
cache:
  directories:
  - node_modules
install:
  - npm install -g now@8.3
  - npm install -g @front-warden/ci-runner-cli
branches:
  only:
   - master
   - /^v\d+\.\d+\.\d+$/
notifications:
  email: false
script:
  - bash ./deploy.sh
